---
title: Color Button
date: 2021-05-23
published: true
cover: ./colors-ic.png
---

A fun React component for changing colors I made while learning an application called Storybook :)

```jsx react-live
function ColorButton() {
  const [colorState, setColorState] = React.useState("red");

  function nextColor() {
    let color = colorState;
    if (color === "red") color = "orange";
    else if (color === "orange") color = "yellow";
    else if (color === "yellow") color = "green";
    else if (color === "green") color = "blue";
    else if (color === "blue") color = "indigo";
    else if (color === "indigo") color = "purple";
    else color = "red";
    setColorState(color);
  }

  return (
    <div className={`color-button ${colorState}`} onClick={nextColor}>
      Choose A Color
    </div>
  );
}
```
