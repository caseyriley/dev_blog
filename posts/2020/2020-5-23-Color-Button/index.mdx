---
title: Color Button
date: 2021-05-23
published: true
cover: ./colors-icons.png
---

A fun React component for changing colors :)

```jsx react-live

function ColorButton() {

    const [colorState, setColorState] = React.useState('red');

    function nextColor(){
        let color = colorState;
        if (color === 'red') color = 'orange';
        else if (color === 'orange') color = 'yellow';
        else if (color === 'yellow') color = 'green';
        else if (color === 'green') color = 'blue';
        else if (color === 'blue') color = 'indigo';
        else if (color === 'indigo') color = 'purple';
        else color = 'red';
        setColorState(color);
    };

    return (
        <div style={{
            display: "flex",
            justifyContent: "center",
            alignItems: "center",
            width: "200px",
            height: "75px",
            borderRadius: "100px",
            backgroundColor: `${colorState}`,
            transition: "all, 1s",
            margin: "20px",
            color: "white",
            fontSize: "22px",
        }} 
        onClick={nextColor} >
            Choose A Color
        </div>
    )

}
```
